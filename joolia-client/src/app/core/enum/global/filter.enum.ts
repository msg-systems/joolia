export enum UserStatusFilter {
    REGISTERED = 'registered',
    PENDING = 'pending'
}

export enum UserRoleFilter {
    ADMIN = 'admin',
    ORGANIZER = 'organizer',
    PARTICIPANT = 'participant'
}
